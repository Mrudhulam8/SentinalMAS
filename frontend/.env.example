# Base URL of the SecureOrch backend API.
# Local dev defaults to http://localhost:8000 if unset.
# In production (Vercel), set this to your deployed backend URL, e.g.
# VITE_API_BASE=https://secureorch-api.onrender.com
VITE_API_BASE=
